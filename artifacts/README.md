# Artifacts

This directory is reserved for generated Mission artifacts such as logs, QA reports, screenshots, traces, bug reports, and regression tests.

Runtime artifact contents are ignored by git. Keep this README tracked so the directory purpose remains visible.
