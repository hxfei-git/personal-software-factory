# Orchestrator API

The Orchestrator API will own Mission state, Project records, WorkerRun records, QA runs, bugs, artifacts, approvals, and event logs.

Phase 1 only reserves the API boundary. Fastify routes and persistence start in later phases.
