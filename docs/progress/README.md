# Progress Notes

This directory stores current progress rollups and focused batch summaries.

Use `../progress.md` as the active progress entrypoint. The old `current.md` Phase 1 note is archived at `../archive/progress/current-phase-1.md` because it no longer represents the repository's current implementation state.
