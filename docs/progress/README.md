# Progress Notes

This directory stores current progress rollups and focused batch summaries.

Use `../progress.md` as the active progress entrypoint. The old `current.md` Phase 1 note is stale and scheduled to be archived by the documentation cleanup archive task because it no longer represents the repository's current implementation state.
