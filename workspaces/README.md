# Workspaces

This directory is reserved for worker-managed local clones and git worktrees.

Runtime workspace contents are ignored by git. Workers must stay inside managed workspaces and must not mutate production or user data without approval.
