# Codex Worker

The Codex Worker will run implementation and fix tasks in isolated branches and worktrees, record logs, run checks, and prepare commits or PRs.

Phase 1 only reserves the worker boundary. Codex CLI integration starts in a later phase.
