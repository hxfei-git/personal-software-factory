{
  "name": "@psf/codex-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal Software Factory Codex Worker dry-run generator.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "pnpm typecheck && pnpm test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@psf/mission-schema": "workspace:*",
    "@psf/artifact-store": "workspace:*",
    "@psf/security": "workspace:*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0",
    "@types/node": "^25.9.1"
  }
}
